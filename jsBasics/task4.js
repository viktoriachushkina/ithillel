// Завдання 4.1: Площа кола
const radiusCircle = 5; // Присвойте значення радіуса
const areaCircle = Math.PI * Math.pow(radiusCircle, 2);
console.log(`Площа кола: ${areaCircle.toFixed(2)}`);

// Завдання 4.2: Площа прямокутника
const lengthRectangle = 10; // Присвойте значення довжини
const widthRectangle = 4; // Присвойте значення ширини
const areaRectangle = lengthRectangle * widthRectangle;
console.log(`Площа прямокутника: ${areaRectangle.toFixed(2)}`);

// Завдання 4.3: Об'єм циліндра
const radiusCylinder = 3; // Присвойте значення радіуса
const heightCylinder = 7; // Присвойте значення висоти
const volumeCylinder = Math.PI * Math.pow(radiusCylinder, 2) * heightCylinder;
console.log(`Об'єм циліндра: ${volumeCylinder.toFixed(2)}`);
